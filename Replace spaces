#include <bits/stdc++.h> 
string replaceSpaces(string &str){
	// Write your code here.
	string rep = "@40";
	for(size_t i =0;i<str.length();i++){
		if(str[i]==' '){
			str.replace(i,1,rep);
			i += rep.length()-1; 
		}
	}
	return str;
}
